import { readFileSync, writeFileSync } from "node:fs";
const raw = JSON.parse(readFileSync("../../deep-dive-2026-07-11/probes/gsheets-raw-results.json", "utf8"));

// verdict + note per id (gsheets branch of each hypothesis)
const V = {
  "arrays-002": ["confirmed", "gsheets F-statistic cell = 2.2536e31 (perfect-fit residual df=0 → finite huge number, not #NUM!); r²≈1, slope≈1, intercept≈0. Matches the ~2.25e31 prediction exactly."],
  "arrays-003": ["confirmed", "gsheets accepts the column-oriented new_x {5;6;7} and spills the 3 projected values as a column [5, 6, 7] (float noise: 5.9999.., 6.9999..). No #REF!."],
  "arrays-004": ["confirmed", "gsheets default-pads the trailing empty WRAPROWS slot with #N/A: [1,2]/[3,#N/A]. Not blank, not 0."],
  "arrays-005": ["confirmed", "gsheets SEQUENCE(2,3,10,5) spills row-major 10,15,20 / 25,30,35 — same start/step semantics as expected."],
  "arrays-006": ["confirmed", "gsheets INDEX(A1:B2,2,1)=20. Re-confirms the fixture; the LibreOffice blank-capture re-record ask is out-of-scope for gsheets."],
  "dve-001": ["confirmed", "gsheets returns 4. Mechanism now pinned: the grid seed A2=\"=1/0\" is a plain string, so the driver classifies it as TEXT and writes it RAW — gsheets stores the literal text `=1/0`, never a live formula. SUM ignores the text cell → 1+3=4. This is exactly why the fixture shows 4 (not #DIV/0!)."],
  "dve-002": ["confirmed", "gsheets WEEKNUM(\"2023-01-01\",21)=52 — coerces the ISO-8601 text to a date serial and applies ISO week type 21."],
  "dve-003": ["confirmed", "gsheets YEARFRAC(text,text,2)=1.0138888888888888 (365/360 actual/360) — coerces both text-date args."],
  "dve-004": ["confirmed", "gsheets genuinely rejects the 5-arg integer-mode RANDARRAY(1,3,1,10,TRUE) with #N/A — not a spill/harness artifact."],
  "dve-005": ["confirmed", "gsheets TIME(25,0,0)=0.041667 (25h reduced mod 24 → 1h). Matches Excel/hyperformula/ironcalc/formulas; lattice's 1.041667 is the sole outlier."],
  "dve-006": ["confirmed", "gsheets LAMBDA(x,x+1)(5)=6 — defines and immediately invokes."],
  "dve-007": ["contradicted", "HEADLINE: hypothesis predicted gsheets broadcasts IFERROR element-wise and spills {10,-1,5}; gsheets actually returned a SINGLE scalar 10 (extent 1x1). Without ARRAYFORMULA, gsheets does NOT map IFERROR over the array 10/{1,0,2} — the error branch (-1) never surfaces and only the first element is returned. The spill/broadcast claim is false for gsheets."],
  "engineering-001": ["confirmed", "gsheets IMLOG(\"8\",2)=\"3\" (string) — IMLOG(value,base) is a gsheets-native two-arg extension."],
  "engineering-002": ["confirmed", "gsheets IMDIV(\"1+2i\",\"0+0i\")=#DIV/0! — zero-divisor error is a property of the divisor value, not its textual form."],
  "engineering-003": ["confirmed", "gsheets IMEXP(\"10+3i\")=\"-21806.035863485+3108.37503049351i\" — components rendered at ~15 significant digits, confirming the cap holds at large magnitude. (Note: the hypothesis's approximate real part \"-21801\" was slightly off; the actual value is -21806.04.)"],
  "engineering-004": ["confirmed", "gsheets DEC2HEX(-1,4)=\"FFFFFFFFFF\" (10 chars) — the places argument is ignored for negative results; two's-complement already fills all 40 bits."],
  "engineering-005": ["confirmed", "gsheets ERF.PRECISE(-0.5)=-0.5204998778130466 (ERF is odd). Agrees with IronCalc/formulas/Lattice to float precision."],
  "engineering-006": ["confirmed", "gsheets IMTANH(\"0\")=\"0\" (string) — IMTANH is a gsheets extension (Excel-absent)."],
  "financial-001": ["confirmed", "gsheets XNPV with ISO-TEXT date args returns #VALUE! — it rejects text dates. (The numeric-serial re-run sub-request was not executed; the primary text-date claim is confirmed.)"],
  "financial-002": ["confirmed", "gsheets XIRR with ISO-TEXT date args COMPUTES 0.06382316999801713 (~0.0638) rather than erroring. Confirms the internal asymmetry: XIRR coerces text dates that XNPV rejects."],
  "financial-003": ["confirmed", "gsheets RRI(0,1000,2000)=#DIV/0! — gsheets alone maps the nper=0 invalid-period failure to #DIV/0! (others return #NUM!)."],
  "financial-004": ["confirmed", "gsheets ACCRINT(...)=295.55555555555554, matching the predicted 295.5555.. branch (diverges from Excel's expected 295.8333 on 30/360 handling of the 31st-of-month issue date)."],
  "financial-005": ["confirmed", "gsheets IPMT(0.05/12,360,360,-200000)=4.454951228316519 — small positive final-period interest, agreeing with hyperformula/ironcalc/lattice."],
  "info-001": ["confirmed", "gsheets CELL(\"format\",A1)=#VALUE! — gsheets rejects the \"format\" info_type."],
  "info-001b": ["confirmed", "gsheets CELL(\"type\",A1)=\"v\" — arg-rejection is per-info_type; CELL(\"type\") works even though CELL(\"format\") is rejected."],
  "info-002": ["confirmed", "gsheets ISBLANK(A1)=FALSE for A1=\"\" — gsheets stores a zero-length string as a genuine value, not a blank cell. Corroborated by stat-core-001 (COUNTA counts it)."],
  "info-002b": ["confirmed", "gsheets ISBLANK(A1)=FALSE for the `=\"\"` seed. CAVEAT: the seed `=\"\"` is a plain string, so the driver stored it as literal TEXT (RAW), NOT a live formula — so this does not cleanly isolate 'live formula yielding empty string' from 'typed empty string'; both are ISBLANK-false in gsheets, but the intended formula-vs-typed disambiguation was not exercised."],
  "info-003": ["confirmed", "gsheets ISDATE(\"2024-01-15\")=TRUE — parses the date string."],
  "info-003b": ["confirmed", "gsheets ISDATE(DATE(2024,1,15))=TRUE — a real date serial is a date."],
  "info-003c": ["confirmed", "gsheets ISDATE(45000)=FALSE — a bare number (even a valid date serial) is NOT a date. ISDATE keys on type/format, not on value-is-date-serial."],
  "info-004": ["confirmed", "gsheets SHEETS(A1)=#N/A — rejects a single-cell reference argument."],
  "info-004b": ["confirmed", "gsheets SHEETS(A1:B2)=#N/A too. Resolves the ambiguity: gsheets SHEETS rejects ALL reference arguments (single-cell AND range) — effectively only the no-arg form is supported; the #N/A is not specific to a degenerate single-cell ref."],
  "lambda-logical-coercion-001": ["confirmed", "gsheets SORT({3;1;2},1,-1) returns ASCENDING [1;2;3] — the third arg is read as is_ascending (-1 coerces to truthy=ascending), unlike Excel's sort_order=-1=descending."],
  "lambda-logical-coercion-002": ["confirmed", "gsheets SORTBY(...)=#NAME? — SORTBY is not exposed in Google Sheets (Excel-family-only at recording time)."],
  "lambda-logical-coercion-003": ["confirmed", "gsheets AND()=#N/A (too few arguments)."],
  "lambda-logical-coercion-004": ["confirmed", "gsheets CHOOSE(5,\"a\",\"b\",\"c\")=#NUM! for the out-of-range index (Excel returns #VALUE!)."],
  "lambda-logical-coercion-005": ["confirmed", "gsheets T(TRUE)=\"\" (empty string), whereas Excel returns a blank cell — the blank-vs-empty-string representation split."],
  "lambda-logical-coercion-006": ["confirmed", "gsheets PRODUCT({\"2\",\"3\",\"4\"})=0 — quoted-number strings inside an array literal are skipped (no factors)."],
  "lambda-logical-coercion-007": ["confirmed", "gsheets SUM(A1:A3)=0 for text-typed cells \"1\",\"2\",\"3\" — numbers-stored-as-text in a range are ignored (not coerced)."],
  "lambda-logical-coercion-008": ["confirmed", "gsheets SUM({TRUE,FALSE,TRUE})=0 — booleans inside an array literal are not coerced (distinct from lattice's 2)."],
  "lookup-001": ["confirmed", "gsheets ADDRESS(1,1,1,TRUE,\"Sheet2\")=\"Sheet2!$A$1\" — a simple sheet name is NOT wrapped in single quotes (unlike the `formulas` engine's 'Sheet2'!$A$1)."],
  "lookup-002": ["unclear", "gsheets FORMULATEXT(A1)=#N/A, BUT the probe seeded A1=42 (a number/value), so it only reconfirms that FORMULATEXT of a VALUE cell is #N/A. The hypothesis's real question — does gsheets return the formula text when A1 holds a LIVE formula — was not tested, because a numeric grid seed cannot create a live-formula cell. Needs a proper live-formula-in-A1 probe."],
  "lookup-005": ["confirmed", "gsheets INDEX(A1:A2,5)=#NUM! for the out-of-bounds row (Excel returns #REF!). The INDEX(...,0)/(-1) follow-ups were not run separately."],
  "lookup-007": ["confirmed", "gsheets SHEET(\"NoSuchSheet\")=#REF! for an unresolvable sheet name. The SHEET()/SHEET(A1) baselines were not run separately."],
  "math-core-001": ["confirmed", "gsheets GT(TRUE,0)=TRUE — booleans rank above all numbers in gsheets cross-type comparison (number < text < boolean)."],
  "math-core-001b": ["confirmed", "gsheets GT(2,TRUE)=FALSE — confirms the boolean-above-number ranking from the opposite argument order."],
  "math-core-002": ["confirmed", "gsheets UNIQUE(\"Apple\",\"apple\",\"APPLE\") spills ALL THREE (case-SENSITIVE dedup), whereas Excel keeps only \"Apple\" (case-insensitive)."],
  "math-core-004": ["contradicted", "HEADLINE: hypothesis predicted gsheets rounds CONVERT to ~9 decimals (3.280839895); gsheets actually returned 3.28083989501312 — ~15 significant digits, diverging from Excel's 3.2808398950131235 only at the ~15th digit (gsheets' 15-sig-fig storage cap), NOT a 9-decimal rounding. If the recorded fixture showed 3.280839895 that was a read-serialization artifact, not gsheets' stored value."],
  "math-longtail-acot-branch": ["confirmed", "gsheets ACOT(-0.5)=-1.1071487177940904 = ATAN(1/-0.5)=ATAN(-2) — confirms gsheets uses the ATAN(1/x) branch (range (-π/2,π/2), negative for x<0), diverging from Excel's (0,π) convention. (The hypothesis's specific figure \"-0.4636\" was a slip — that is ATAN(-0.5), not ATAN(1/-0.5); the branch mechanism is confirmed.)"],
  "math-longtail-acot-branch-large": ["confirmed", "gsheets ACOT(-100)=-0.009999666686665238 = ATAN(1/-100), approaching 0 from below — confirms the ATAN(1/x) branch limit."],
  "math-longtail-acot-zero": ["confirmed", "gsheets ACOT(0)=1.5707963267948966 (π/2) — conventions agree at 0."],
  "math-longtail-acoth-domain": ["confirmed", "gsheets ACOTH(0.5)=#NUM! — |x|<1 is outside the domain."],
  "math-longtail-improper-complex-digits": ["confirmed", "gsheets IMLN(\"2+3i\")=\"1.28247467873077+0.982793723247329i\" — ~15 significant digits per component (mantissa-precision rendering axis)."],
  "math-longtail-improper-i-residue": ["confirmed", "gsheets IMPOWER(\"i\",2)=\"-1+1.22464679914735E-16i\" — the uppercase-E imaginary residue is retained (not cleaned to \"-1\")."],
  "math-longtail-munit-zero": ["confirmed", "gsheets MUNIT(0)=#NUM! (Excel returns #VALUE!) — the invalid 0x0 identity-matrix error-code split."],
  "math-longtail-formulas-randbetween": ["confirmed", "gsheets ISNUMBER(RANDBETWEEN(1,10))=TRUE — integer output contract holds (the `formulas` engine violates it by returning 3.535)."],
  "math-longtail-libre-rerun": ["confirmed", "gsheets SIN(0)=0 (trivial). The actionable ask (LibreOffice blank-capture re-run) is out-of-scope for gsheets."],
  "spill-broadcast-001": ["confirmed", "gsheets INDEX(A1:A3*10) spills the full column [10;20;30]."],
  "spill-broadcast-002": ["confirmed", "gsheets INDEX({1,2,3}+{10;20;30}) spills the full 3x3 outer-product grid [11,12,13]/[21,22,23]/[31,32,33] — row-vector + column-vector broadcasts."],
  "spill-broadcast-003": ["confirmed", "gsheets LEN(B1:B3)*1 returns a SINGLE scalar 3 (implicit intersection, no ARRAYFORMULA), whereas Excel auto-spills [3;3;3]."],
  "spill-broadcast-004": ["contradicted", "HEADLINE: hypothesis predicted BOTH engines pad the non-overlapping cell with #N/A → [11,22,#N/A]; gsheets actually returned a SINGLE scalar 11 (extent 1x1). For two mismatched-length ROW vectors {1,2,3}+{10,20}, gsheets does NOT spill-with-#N/A-padding — it returns only the first element. (Contrast spill-broadcast-002 where row+COLUMN vectors DO broadcast to a 3x3 grid.)"],
  "spill-broadcast-005": ["confirmed", "gsheets SEQUENCE(3,2) spills [1,2]/[3,4]/[5,6]."],
  "spill-broadcast-006": ["confirmed", "gsheets SORT(SEQUENCE(5),1,-1) returns ASCENDING [1;2;3;4;5] — reads -1 as is_ascending=truthy=TRUE. Core evidence for the SORT sort_order arg-semantics divergence."],
  "spill-broadcast-007": ["confirmed", "gsheets SUM(1,2,3)=6 (trivial). LibreOffice re-run is the actionable ask."],
  "spill-broadcast-008": ["confirmed", "gsheets SORT({3;1;2},1,-1) returns ASCENDING [1;2;3] on a bare array literal — clean control confirming the sort_order divergence."],
  "stat-analytics-001": ["confirmed", "gsheets MARGINOFERROR(A1:A5,0.05)=0.04718417110127355 — a live gsheets function equal to CONFIDENCE.T(0.05,STDEV,COUNT). Not a recording artifact."],
  "stat-analytics-002": ["confirmed", "gsheets HYPERLINK(\"https://example.com\",\"click me\") has scalar value \"click me\" (the display label), joining the string class."],
  "stat-analytics-003": ["confirmed", "gsheets CONFIDENCE(0.05,1,100)=0.1959963986120195 vs Excel's 0.19599639845400538 — an ~8th-sig-fig difference from gsheets' NORM.S.INV approximation."],
  "stat-core-001": ["confirmed", "gsheets returns \"3|1\": COUNTA counts the \"\" cell (=3, treats empty string as a value) AND COUNTBLANK also counts it (=1). Matches the predicted gsheets branch (contrast Excel's \"2|1\")."],
  "stat-core-002": ["confirmed", "gsheets PERCENTRANK.EXC(A1:A5,1)=0.167 (3-sig-digit default) and with significance=6 gives 0.166667 — confirms the 0.166-vs-0.167 default-precision split resolves to the true 1/6 at higher precision."],
  "stat-distributions-001": ["confirmed", "gsheets CHISQ.TEST(single-row)=0.3678794411714419 = CHISQ.DIST.RT(2,2)=1/e — consistent with a df=cols-1=2 fallback for the degenerate single-row table."],
  "stat-distributions-002": ["confirmed", "gsheets CHISQ.TEST with a 2x3 observed vs 2x2 expected shape mismatch returns #N/A (ranges must match shape)."],
  "stat-distributions-003": ["confirmed", "gsheets BETA.DIST(1.5,2,2,TRUE,0,1)=#NUM! — x=1.5 above the upper bound 1 is out of support."],
  "stat-distributions-004": ["confirmed", "gsheets NORM.S.INV(0.99)=2.326347874388028 vs the reference 2.3263478740408408 — differs at the ~9th-10th significant digit (~3.5e-10), reconfirming gsheets' inverse-normal quantile drift."],
  "text-regex-001": ["confirmed", "gsheets REGEXEXTRACT(\"2025-03-01\",\"(\\d{4})-(\\d{2})-(\\d{2})\") returns the capture groups [\"2025\",\"03\",\"01\"] (spilled as a row) — gsheets returns capture groups by default, unlike Excel which returns the full match."],
  "text-regex-003": ["confirmed", "gsheets MIDB(\"あいう\",3,2)=\"う\" — MIDB is character-based here. (The LENB/FINDB/REPLACEB byte-vs-char cross-checks in the note were not run as separate formulas.)"],
  "text-regex-004": ["confirmed", "gsheets CHAR(0)=#NUM! (code 0 out of 1..255), whereas Excel returns #VALUE! — error-code split."],
  "text-regex-006": ["confirmed", "gsheets DOLLAR(-1234.5,2)=\"-$1,234.50\" (leading minus), whereas Excel renders accounting parentheses ($1,234.50)."],
};

const missing = raw.results.filter((r) => !V[r.id]).map((r) => r.id);
if (missing.length) throw new Error("no verdict for: " + missing.join(", "));

const out = raw.results.map((r) => ({
  id: r.id,
  formula_used: r.formula_used,
  outcome: r.outcome,
  verdict: V[r.id][0],
  note: V[r.id][1],
}));
writeFileSync("../../deep-dive-2026-07-11/probes/gsheets-results.json", JSON.stringify(out, null, 2));

const vc = {};
for (const r of out) vc[r.verdict] = (vc[r.verdict] || 0) + 1;
console.log("total:", out.length, "verdicts:", JSON.stringify(vc));
console.log("non-confirmed:", out.filter((r) => r.verdict !== "confirmed").map((r) => r.id + "=" + r.verdict).join(", "));
