schemaVersion: 3
name: Logical functions
tests:
  - subject: IF
    subjectRef: IF
    formula: =IF(TRUE, 1, 2)
    category: value
    expect: 1
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: if-true-branch
  - subject: IF
    subjectRef: IF
    formula: =IF(FALSE, 1, 2)
    category: value
    expect: 2
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: if-false-branch
  - subject: IF
    subjectRef: IF
    category: value
    formula: =IF(2>3, TRUE, FALSE)
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    tags:
      - divergence
    name: if-with-comparison
  - subject: IF
    subjectRef: IF
    category: value
    status: observed
    formula: =IF(2>3, TRUE)
    tags:
      - divergence
    name: if-two-args-false
  - subject: IF
    subjectRef: IF
    category: value
    formula: =IF(1>2, "a", IF(3>2, "b", "c"))
    expect: b
    name: if-nested
  - subject: AND
    subjectRef: AND
    formula: =AND(TRUE, TRUE, TRUE)
    category: value
    expect: true
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: and-all-true
  - subject: AND
    subjectRef: AND
    formula: =AND(TRUE, FALSE, TRUE)
    category: value
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: and-one-false
  - subject: AND
    subjectRef: AND
    category: value
    formula: =AND(1, 1, 0)
    expect: false
    name: and-with-numbers
  - subject: AND
    subjectRef: AND
    category: value
    status: observed
    formula: =AND()
    tags:
      - divergence
    name: and-empty
  - subject: OR
    subjectRef: OR
    formula: =OR(FALSE, TRUE, FALSE)
    category: value
    expect: true
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: or-one-true
  - subject: OR
    subjectRef: OR
    formula: =OR(FALSE, FALSE, FALSE)
    category: value
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: or-all-false
  - subject: OR
    subjectRef: OR
    category: value
    formula: =OR(0, 0, 1)
    expect: true
    name: or-with-numbers
  - subject: NOT
    subjectRef: NOT
    formula: =NOT(TRUE)
    category: value
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: not-true
  - subject: NOT
    subjectRef: NOT
    formula: =NOT(FALSE)
    category: value
    expect: true
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: not-false
  - subject: XOR
    subjectRef: XOR
    formula: =XOR(TRUE, TRUE, TRUE)
    category: value
    expect: true
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: arg-semantics
        recorded: 1
    name: xor-odd-trues
  - subject: XOR
    subjectRef: XOR
    formula: =XOR(TRUE, TRUE)
    category: value
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: arg-semantics
        recorded: 0
    name: xor-even-trues
  - subject: XOR
    subjectRef: XOR
    formula: =XOR(FALSE, FALSE)
    category: value
    expect: false
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: arg-semantics
        recorded: 0
    name: xor-all-false
  - subject: IFERROR
    subjectRef: IFERROR
    category: error-code
    formula: =IFERROR(1, "error")
    expect: 1
    name: iferror-no-error
  - subject: IFERROR
    subjectRef: IFERROR
    category: error-code
    formula: =IFERROR(1/0, "error")
    expect: error
    name: iferror-with-error
  - subject: IFERROR
    subjectRef: IFERROR
    category: value
    formula: =IFERROR(IFERROR(1/0, 1/0), "both failed")
    expect: both failed
    name: iferror-nested
  - subject: IFNA
    subjectRef: IFNA
    formula: =IFNA(1, "na")
    category: error-code
    expect: 1
    overrides:
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: ifna-no-error
  - subject: IFNA
    subjectRef: IFNA
    formula: =IFNA(MATCH(99, {1,2,3}, 0), "not found")
    category: value
    expect: not found
    overrides:
      libreoffice:
        cause: error-code
        recorded:
          error: "#N/A"
    tags:
      - bare-formula
    name: ifna-with-na
  - subject: SWITCH
    subjectRef: SWITCH
    formula: =SWITCH(1, 1, "one", 2, "two")
    category: value
    expect: one
    overrides:
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: switch-match-first
  - subject: SWITCH
    subjectRef: SWITCH
    formula: =SWITCH(2, 1, "one", 2, "two")
    category: value
    expect: two
    overrides:
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: switch-match-second
  - subject: SWITCH
    subjectRef: SWITCH
    formula: =SWITCH(99, 1, "one", 2, "two", "other")
    category: value
    expect: other
    overrides:
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: switch-with-default
  - subject: SWITCH
    subjectRef: SWITCH
    formula: =SWITCH(99, 1, "one", 2, "two")
    category: value
    expect:
      error: "#N/A"
    overrides:
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
      pycel:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: switch-no-match-no-default
  - subject: IFS
    subjectRef: IFS
    formula: =IFS(TRUE, "a", TRUE, "b")
    category: value
    expect: a
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: ifs-first-match
  - subject: IFS
    subjectRef: IFS
    formula: =IFS(FALSE, "a", TRUE, "b")
    category: value
    expect: b
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: ifs-second-match
  - subject: IFS
    subjectRef: IFS
    formula: =IFS(FALSE, "a", FALSE, "b")
    category: value
    expect:
      error: "#N/A"
    overrides:
      hyperformula:
        cause: missing-function
        recorded:
          error: "#NAME?"
      libreoffice:
        cause: missing-function
        recorded:
          error: "#NAME?"
    name: ifs-no-match
  - subject: CHOOSE
    subjectRef: CHOOSE
    category: value
    formula: =CHOOSE(1, "a", "b", "c")
    expect: a
    name: choose-first
  - subject: CHOOSE
    subjectRef: CHOOSE
    category: value
    formula: =CHOOSE(3, "a", "b", "c")
    expect: c
    name: choose-third
  - subject: CHOOSE
    subjectRef: CHOOSE
    category: value
    status: observed
    formula: =CHOOSE(5, "a", "b", "c")
    tags:
      - divergence
      - error-type
    name: choose-out-of-range
