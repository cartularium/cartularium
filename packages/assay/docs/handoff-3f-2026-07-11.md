# Session handoff — 3f reclassify (2026-07-11)

> **Re-founded 2026-07-18.** This document is design history. Labels such as
> "ratified" or "charter" inside it carry no authority; governing decisions
> live in the internal decisions ledger (see
> `internal/decisions/2026-07-18-assay-refounding.md`). Where this document
> describes the no-verdict frame it remains an accurate description; where it
> conflicts with the re-founding decisions, the decisions win.

Worktree `assay-floor`, branch `feat/assay-floor-hardening` (branched from
`integrate/assay-onto-main` @ `64eaaf8d`; the `assay-batch-model` worktree paused at "RESUME —
3f" and this branch picks that thread up — coordinate before merging either). All work LOCAL;
push maintainer-gated. Baseline suites were green before work began (contracts / assay 168 /
edit-shell 123).

**Read order:** `reclassify-policy-2026-07-11.md` (the 3f policy, PROPOSED — D-3f-1..7 with the
measured ground data) → this log.

## Shipped (committed)

1. **3f tooling** — YAML `scope:` sugar (loader parse+validate, fail-fast; refs XOR
   tags/subjectIn), seed-exporter precedence (authored scope wins over the `tests` ref-set
   fallback), corpus gate test (scope refs + subjectIn subjects must resolve), 5 parser tests.
2. **Manifest universe widening (D-3f-4 — the session's biggest finding).** The V5 builder
   inherited a V4-era `isFunctionName` gate that silently dropped every non-function-subject
   case (`op:*`/`lit:*`/`feature:*`/`legacy:*`) — 88 tests, 86 with live observations — from the
   published relation layer. The 3d "danglings" were these clipped cases, NOT renames. Gate
   removed; `functions` rollup unaffected. Effect: manifest tests 1867→1955, forks 1822→**1908**,
   dangling annotations 17→**0**, no-live-fork 22→**9** (all genuinely converged; kept per
   D-3f-5).
3. **Hygiene-gate leak fixed** — `engine-divergence` (published on 10 regex cases) joined the R1
   outcome-claim denylist.
4. **3f.3 conversions** — 17 exact-alignment DVs (16 missing-function + shape/SORTN) converted
   to `subjectIn` predicate scopes; DV-0203/0239/0243 deliberately kept ref-set (case-level
   mechanisms). Verified: re-seeded local D1 (255 rows, 17 predicate-scoped), coverage identical
   (962 covered) with predicates resolving in place of ref-sets.

## 3f.5 authorship fan-out — DONE (completed after the quota reset)

All 48 batches completed on resume; post-processed deterministically → **124 new annotations
(`DV-0258..DV-0381`) covering 688 of the 946 uncovered forks**. Coverage **51.9% → 86.5%**
(1650/1908). The other **258 refs were QUARANTINED, not authored**: every libreoffice
blank-cell reading (engines=[libreoffice], cause recalc-semantics/null-vs-zero) — a suspected
driver recalc artifact under investigation by a parallel agent
(`docs/libreoffice-recalc-2026-07-11.md` when it lands); authoring against that evidence would
go stale on fixture regen. Quarantine bucket: `<scratchpad>/quarantine-lo-blank.json`. If the
artifact is REFUTED, re-run the post-processor without the quarantine filter to author them;
if CONFIRMED, regen fixtures → those forks reshape → re-run coverage → author what remains.

Gates all passed: assay 176 green · local D1 seeded 379 rows · 0 unresolved predicate clauses ·
0 denylist leaks · V4 site builds. subjectIn strictly verified: 3 accepted of 10 agent-claimed.

<details>
<summary>Original resume coordinates (consumed)</summary>


The 946 uncovered forks were packeted into 48 suite batches
(`<scratchpad>/packets/*.json`) and a 48-agent workflow authored annotations from the evidence.
**19/48 batches completed (~636 refs: math-longtail, statistical-distributions, engineering,
info, broadcasting, statistical-descriptive, math), then the session usage limit hit (resets
3:20am PT).** Everything needed to finish:

- Partial results: `<scratchpad>/wf-partial-19.json` (batch-labelled).
- Resume: `Workflow({scriptPath: <session workflows dir>/author-fork-annotations-wf_b19e0107-d57.js,
  resumeFromRunId: "wf_b19e0107-d57", args: <the 48-batch list>})` — the 19 replay from cache,
  29 run fresh. (Scratchpad + workflow scripts are session-scoped; if lost, regenerate packets
  with the snippet in the policy doc §0 and re-run — the prompt is in the script file.)
- Then: `node <scratchpad>/postprocess.mjs <output.json> [--write]` from `packages/assay` —
  validates (cause enum, refs within packet, no double-claim, unclaimed-ref report), merges
  same-(missing-function × engine-set) groups across batches, verifies `subjectInSafe` claims
  strictly (refs == ALL forked refs of the subjects), assigns `DV-0256+`, emits YAML.
- Then the §8 validation gates: suites green → `seed-fork-annotations` → chunked local-D1 apply
  → export → `annotation-coverage` (uncovered should drop from 946 toward ~0; predicates 0
  unresolved; 0 denylist leaks) → V4 site builds → idempotent re-seed.

</details>

## LibreOffice recalc artifact — CONFIRMED + fixed + purged (post-fan-out)

The parallel investigation CONFIRMED the artifact (`libreoffice-recalc-2026-07-11.md`):
**1844/1921 libreoffice fixture entries are fabricated blanks** — the driver's
openpyxl→soffice-convert→openpyxl path never recalculated (headless soffice's recalc-on-load
prompt is a silent no-op). Driver fixed (`32607044`: fresh profile with
ODFRecalcMode=0/OOXMLRecalcMode=0 via -env:UserInstallation). **Needs the maintainer (no
soffice on this box): one live verify run, then regen all 32 suites' libreoffice fixtures**
(`node build/cli.js generate tests/<suite>.yaml --platform libreoffice`).

Downstream purge, done: audited all 124 authored DVs for lo claims resting on blank-class
membership — DV-0320 deleted (all 23 refs were forks fabricated solely by lo's phantom blank
class), 39 DVs edited (lo de-listed from engines + excised from summaries; their forks
survive on real other-engine evidence). Local D1 re-seeded (378 rows). Coverage **85.3%**
(1627/1908); uncovered 281 = 258 quarantined + 23 from DV-0320, all pending post-regen
re-author. After the maintainer regens fixtures: rebuild manifest → re-run coverage → author
whatever genuinely remains (the quarantine bucket + these 281 dissolve or reshape).

**Authoring quality note for the verification queue:** the completed batches read well
(symmetric, evidence-grounded, controlled causes). One systematic reading to double-check:
agents repeatedly found **libreoffice surfacing blank cells where every other engine computes a
value** (whole stretches of math-longtail) and read it as recalc-semantics/null-vs-zero —
plausibly a **driver artifact** (fixture written without recalculation on load) rather than an
engine behavior. That is a driver-fidelity lead: if confirmed, those fixtures are
seed-infidelity/no-data, not value observations, and the annotations self-dissolve. Worth a
libreoffice-driver probe before verifying those annotations.

## Fixture regen + 100% coverage (session close)

LibreOffice 26.2.4.2 installed (brew cask); the recalc fix **live-verified** (math-longtail:
187 blank → 0 blank) and all 32 suites regenerated (1921/1921 ok, 22s). 1844 fabricated
blanks → 11, all genuine empty-string results (=T(42), =REPT("ha",0) — the known
blank-vs-empty-string capture territory, cf. D8.β). **286 fabricated forks dissolved
(1908 → 1622)**; the entire quarantine (258 refs) + DV-0320's 23 refs self-resolved. Three
genuine forks the regen surfaced were authored (DV-0382 TODAY run-date skew · DV-0383
libreoffice VALUE("TRUE")→1 · DV-0384 SEQUENCE missing ×5 engines).

**Final state: 1622 forks, 1622 covered (100.0%), 0 uncovered.** 381 annotations in local
D1; 24 cover no live fork (converged — kept, descriptive). All gates green (assay 176 ·
denylist 0 · V4 site builds). Note for verification passes: volatile-function fixtures
(TODAY/NOW/RAND) fork across non-simultaneous runs by nature — DV-0382 records one such;
a simultaneity policy for volatile stimuli is a possible future measure refinement.

## Operational notes

- `wrangler d1 execute --file` (wrangler 4.86) fails with `SQLITE_TOOBIG` on the multi-statement
  seed file — a statement-splitting regression (3c verified the same flow live on an earlier
  wrangler). Workaround: strip comments, split into ~32-statement chunks (snippet in the shell
  history / trivially rederivable); or pin wrangler.
- Local D1 in THIS worktree is migrated (0001–0008) and seeded (255 rows incl. the 17 predicate
  scopes).
- Coverage inputs used throughout: `<scratchpad>/manifest-v5-widened.json`,
  `annotations-export2.json`, `coverage2.json`.

## Next after 3f.5 lands

- **Store→build delivery — DESIGNED + REFERENCE-IMPLEMENTED this session**
  (`store-delivery-2026-07-11.md`): the public read lane `GET /api/assay/fork-annotations`
  (published-only export, sessionless, CORS-open — the shape `annotation-coverage` consumes)
  and `GET /api/assay/fork-coverage` (R2 manifest × published annotations via
  `computeForkCoverage`; 503+hint until a manifest is published to
  `cartularium-assay-preview/assay/manifest-v5.json`). edit-shell 127 tests green, tsc clean.
  Remaining for #4 (deliberately deferred with the sheets-wiki V4→V5 rework): the committed
  snapshot command + re-pointing the ~4 `loadDvs()` read sites.
- The maintainer verification queue (`?verified=false`) now includes everything this pass
  authored — all `auto-seeded (provisional)`, `verified_* = NULL`, by design.
- **Community classification design** (session-close brainstorm with the maintainer):
  `community-classification-2026-07-11.md` — PROPOSED. Flat store / open attestation / named
  default lens / evidence-consistency checker; prior-art survey; nothing to build now, only
  schema discipline to keep (no rank/tier fields, attestations stay per-person snapshot rows).
