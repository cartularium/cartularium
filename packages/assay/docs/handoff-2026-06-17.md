# Session handoff — 2026-06-17

A long, deep session on `integrate/assay-onto-main` (worktree `assay-batch-model`).
Theme: a **philosophy rework** (no-verdict / multiplicity) + **CP1 of the comparison
initiative** (close the floor). For fresh review, read in this order:
`terminology.md` → `comparison-initiative-2026-06-17.md` → this log.

## What we did (in order)

1. **Oriented.** Branch was 2 commits ahead of `main` (M3 slice 1 + `semanticDomain`
   dissolution), both local. Located a stale handoff the maintainer half-remembered —
   confirmed it was the June-15 note, fully superseded.
2. **Code review (medium) of the 2 unpushed commits.** Found one real regression +
   cleanups. **Regression: dropping `semanticDomain` changed the `semanticHash` of 69
   cases → orphaned their recorded fixtures** (verified `main` 1955/1955 → HEAD 1886+69).
   Deferred to the regen (it heals by re-keying). Cleanups applied: shared the canonical
   `VOLATILE_SUBJECTS` (killed a 4th duplicate literal; the helper was load-bearing for 12
   deterministic-volatile cases, so *not* removed); union-find skip-guard in `relations.ts`;
   a hash-identity comment in `semantic-hash.ts`.
3. **Terminology lexicon (`terminology.md`).** Ratified: the **two-layer cut** (relation
   layer = descriptive/verdict-free; lens layer = owned normativity). Comparison frame =
   **MULTIPLICITY**, not conformance — a formula is **uniform** (1 agreement-class) or
   **forked/plural** (>1, each a legitimate branch). `divergence` RETIRED (a fossil of the
   pre-lattice "Excel+gsheets agreeing = the reference" frame). Blessed/banned vocabulary +
   homonym keep-list (`canonicalize` ≠ `canon`). The comparison cluster (`classify.ts`/matrix/
   benchmark) **quarantined** pending CP2.
4. **Step-back assessment** (3 parallel inventories). The floor (contracts value spine +
   drivers) is ~solid; the comparison model is **split-brain** (verdict-free partition built
   but DEAD at the output boundary); the hub has the fossil **already exported** (sheets-wiki
   reads `ManifestV4.TestVerdict`) and the bridge **unbuilt** (interleaf hardcoded). Keystone =
   the comparison-OUTPUT contract.
5. **Scoped the initiative** → `comparison-initiative-2026-06-17.md`: CP1 close-the-floor,
   CP2 comparison-output contract, CP3 migrate+wire the hub. Maintainer ruled the **website is
   NOT a migration gate** (will be reworked, not preserved).
6. **CP1 — DONE.** Live full-corpus regen on the ratified model held; **147 tests green**;
   excel 1955/1955, gsheets 1950/1955, pure engines refreshed; 69 orphans healed (6 engines).
   **Fixed a first-class gsheets driver bug** (read packed ~1250 ranges into one
   `spreadsheets.get` URL → 400 HTML at scale; bounded ranges/GET). Tail (NOT floor gaps):
   lattice/libreoffice un-regenerated (no env); gsheets 404 default-id (used a fresh scratch;
   2 scratch sheets leaked); `lift.ts` still live for the 6 lifted engines.
7. **CP2 — started.** The **three-axes** decomposition (relation / capability / oracle — the
   un-smushing of `TestVerdict`); the dead/rework/keep sweep; grounded both contracts on the
   regen data. Surfaced the **open fork** (below).

## Current state

- **Branch:** `integrate/assay-onto-main`, **7 commits ahead of `main`, all LOCAL** (push
  still gated by the maintainer). Working tree clean (after this handoff's doc/memory writes).
- **Commits this session:** code-review cleanups → terminology+initiative docs → gsheets fix →
  CP1 regen (192 fixtures) → CP3 website note. (Plus pre-session M3 slice 1 + semanticDomain.)
- **Tests:** 147 assay green; 92 drivers green.

## RESUME HERE — the CP2 open fork (sleep-on-it)

**Where does an authored assertion live?** An `expect` on the test (even optional) is a
"correct" in the catalogue, which the no-verdict principle forbids. Leaning: **test = pure
observation; assertion = external, consumer-owned lens.** Reconciliation floated: `expect` as
authoring sugar that **compiles out** to an external lens (pure catalogue, ergonomic authoring).
Decide from principle, not corpus density. Dependency: `deriveCategory` reads `expect` today.

Once that lands, CP2 proceeds to design ManifestV5's per-test shape (the three axes explicit:
`classes` partition + per-engine capability + optional external oracle), and whether the interleaf
feed is a projection of V5 or its own shape.

## Backlog (full inventory in this session's discussion)

- **Custom test format = D8** (YAML vs cell-grammar, parked, `seeding-isolation-design`) + D1′ seed-sugar.
- gsheets: **404 default spreadsheet id**; **`drive.file` scope** (delete bricked orphans + the 2 leaked
  scratch sheets); a **regression test** for the read sub-batching fix.
- Floor tail: **lattice + libreoffice regen** (need their environments); `lift.ts`/`legacyToOutcome` retirement.
- Deferred comparison machinery: the **digest** (Move 1), the result **fingerprint** (equality #5), the **O4**
  probe, persistent-effect remedies + reuse-isolation.
- Open design: D7 (pycel/libreoffice chunk bounds), D2 gating probes, D8.β, value-model collapse, Ring 2.
- Review findings deferred: `supportLevel` load-bearing for exclusion (latent); `supportLevel` enum validator.
- CP3 work: re-seat classify/manifest/catalogue/matrix; `divergence→forked`; §4 quarantine sweep + the 3
  docs still carrying canon/expect framing; wire interleaf feed.
- M3 relation seams: stability relation, input-fidelity relation, flat provenance, consumer lenses, rung projections.
