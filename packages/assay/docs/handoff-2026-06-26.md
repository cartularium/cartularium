# Session handoff — 2026-06-26

Worktree `assay-batch-model`, branch `integrate/assay-onto-main`. Theme: **CP3 increment #3 —
the fork-annotation store**. The maintainer greenlit the 3a design; this session resolved its open
items, built 3b (the store), and ran a code review with fixes. All LOCAL (push maintainer-gated),
tree clean.

**Read order for next session:** this log → `annotation-store-design-2026-06-20.md` (the ratified
3a design; §10 sequencing, §11 resolved decisions) → `comparison-output-contract-2026-06-17.md`
(the manifest is observation-only) → the comparison-principles memory.

## What we did (in order)

1. **3a RATIFIED** (`6ee8dd38`). Folded the maintainer's verdicts into
   `annotation-store-design-2026-06-20.md` and flipped `PROPOSED → RATIFIED`:
   - **§8** edit-shell → `@cartularium/contracts` edge **CONFIRMED**.
   - **§9 / R4** review gate = **OPTION B (light review)** — a publication `status`
     (`pending` / `published` / `rejected`), **moderation/hygiene only, never correctness-
     vouching** (would re-break no-authority). API shaped so flipping to publish-on-sign is
     trivial once the tiered "attributed-but-unreviewed" render exists.
   - **R1** publishing manifest `tags` = a hygiene **GATE** (not a one-time sweep); 3e work.
   - **R2** the in-repo DV-lifecycle does **NOT** retire fully — it's coupled to an undesigned
     results-history substrate (keyed by the same case-ref the scope uses, able to tell a rename
     from a convergence); deferred to #4.
   - **R3** the coverage view (3d) distinguishes a **dangling ref** from a converged fork.

2. **3b SHIPPED** (`5cd012ac`) — contracts DTO + D1 migration + CRUD API:
   - **contracts** `assay-fork-annotation.ts`: `AssayForkAnnotationV1` (canonical record, incl.
     `status`), `AssayForkAnnotationInput` (authored surface, no status), `AnnotationScope`
     (clause LIST, unioned), `ScopeClause` (`ref-set` | `predicate`), `ForkPredicate`,
     `AssayForkAnnotationStatus`, `ASSAY_FORK_ANNOTATION_VERSION = 1`. Also refactored `Cause`
     to derive from a single `ALL_CAUSES` list (+ `isCause`), mirroring `ALL_CATEGORIES` — same
     union, one source of truth so the API can validate it.
   - **edit-shell** (its FIRST `@cartularium/contracts` dependency): migration
     `0007_assay_fork_annotations` (`scope_json`; `status` CHECK + `DEFAULT 'pending'`) +
     `routes/assay-fork-annotations.ts` mounted at `/api/edit/assay/fork-annotations`. Endpoints:
     `GET /` (list; visibility = maintainers all, others published + own), `GET /:id`,
     `POST /` (creates `pending`, author = session user), `PATCH /:id` (own/maintainer),
     `DELETE /:id` (own/maintainer), `POST /:id/review` (maintainer publish/reject). valibot-
     validated; `isAssayMaintainer` reused (now exported) from `assay-preview.ts`.

3. **Code review + fixes** (`f0ef91b2`). Three finder agents (route correctness / cross-file +
   routing / contracts + migration + conventions). **Core verified sound**: SQL bind ordering,
   GET visibility, valibot key-stripping, no injection, the re-moderation gate, Hono mount
   ordering (no shadowing), the `Cause` refactor (byte-identical), rate-limit. Six findings fixed:
   - **Build-before-consume unwired** (medium; found twice): edit-shell now runtime-imports
     contracts but nothing built it first → fresh-clone/CI failure (a stale build → `ALL_CAUSES`
     undefined → `v.picklist` breaks at module load). Prefixed dev/deploy/test/typecheck with the
     contracts build (mirrors `drivers`/`assay`); added a `check` script so root `pnpm check`
     covers edit-shell.
   - **Rejected edits were stuck** (medium): generalized re-moderation to `status !== "pending"`,
     so a non-maintainer author editing a published OR rejected row re-enters review.
   - **403/404 existence leak** (low): added `canSee`/`canWrite` — not-visible → 404 everywhere,
     visible-but-not-yours → 403, consistent with GET.
   - **`cause` uncleanable** (low): `PatchBody.cause` now nullable (null clears, omitted leaves).
   - **Whitespace scope tokens** (low): refs/tags/subjectIn trim+nonEmpty; sentinel nonEmpty.
   - **Stale `edit-shell/CLAUDE.md`** boundary rule updated to record the contracts edge.

## Current state

- **Branch** `integrate/assay-onto-main`; 3 commits this session (`6ee8dd38`, `5cd012ac`,
  `f0ef91b2`), **all LOCAL**. Tree clean.
- **Green**: contracts 43, edit-shell 120, assay 158; `tsc` + prettier clean; whole-repo
  `pnpm check` passes (now includes edit-shell).
- 3a design is RATIFIED; 3b (DTO + migration + CRUD API) is built, reviewed, and tested. The DV
  import (3c) has NOT started — deliberately saved for next session.

## RESUME — 3c (the one-time DV import)

A maintainer-run, idempotent import (design doc §5):

> read `loadDvs()` → one row per `DV-####`: `id = DV-####`,
> `author_id = "auto-seeded (provisional)"`, `content = summary`, `cause = cause`,
> `scope = [{ kind: "ref-set", refs: tests }]`, `status = 'published'` (existing catalogue
> content, not a new contribution). `engines`/`category` NOT copied (derived at read time).
> Mechanism: `INSERT … ON CONFLICT(id) DO UPDATE`, idempotent on `id`.

Notes for the build:
- The 10 `vanished` DVs import as plain rows; they simply match no current fork (3d adds the
  dangling-ref signal — don't special-case them here).
- It touches nothing in-repo: the YAML / `seedCatalogue` / `history` stay (the V4 site still
  renders them); they retire with #4.
- The join key is already synced (edit-shell `submittedSemanticHash()` ↔ assay
  `format/semantic-hash.ts`; `canonical_case_id` = the `SUBJECT/name` ref `DV.tests` holds).
- Where the import script lives / how it's invoked is the first 3c decision (a maintainer CLI in
  edit-shell vs an assay-side exporter feeding the API). The migration table + CRUD API exist.

## After 3c (design doc §10)

- **3d** coverage views (+ the R3 dangling-ref signal).
- **3e** publish manifest `tags` (with the R1 hygiene gate) + resolve tag-predicates.
- **3f** reclassification (provisional ref-sets → tag/predicate scopes; likely workflow-automated).
- **Deferred**: the observed-property fork matcher; the stability computation + results-history
  substrate (R2); sheets-wiki V4→V5 + retiring the in-repo DV YAML / `history` / `seedCatalogue`
  (#4 — not a migration gate, reworked not preserved).

Push is still maintainer-gated (all session work local).
