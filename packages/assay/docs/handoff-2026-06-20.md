# Session handoff — 2026-06-20

Worktree `assay-batch-model`, branch `integrate/assay-onto-main`. Theme: **CP3 of the
comparison initiative** — two build increments shipped (the V5 manifest CLI + the matrix
re-source), then a deep **design session on DV-identity** that resolved into re-placing the
whole annotation/cause layer (contributed, out-of-band, in edit-shell).

**Pickup plan (maintainer): a brief gap-review first, then likely implementation.** Read in
this order: `terminology.md` → `comparison-initiative-2026-06-17.md` →
`comparison-output-contract-2026-06-17.md` (incl. the 2026-06-19 refinement note) →
`annotation-layer-design-2026-06-19.md` → this log.

## What we did (in order)

1. **CP3 step 5 — the `manifest` CLI now publishes ManifestV5** (`4bd4c8d5`). Added
   `loadFixtureOutcomes` (the §6.6 outcome-aware sibling of `loadFixtures`) + extracted
   `liftEntryToOutcome` (the single back-compat lift, shared). Re-seated `commands/manifest.ts`
   on `buildManifestV5`. **Validated live on the full corpus** (520 fns / 1867 tests): the V5
   producer is structurally correct + partitions faithful to fixtures. The 98% fork rate is
   honest, not a bug (error-vs-value coverage gaps + the libreoffice/lattice un-regen blank tail).
2. **CP3 step 6 — re-seated the matrix reporting on the observed partition** (`f7634a7c`).
   Found mid-step that the matrix is the **identity spine of the DV-lifecycle history**
   (`history/record.ts`→`computeMatrix`→`diffClusters` keys DV files by the override fossil).
   Scope decision (maintainer): re-source the **reporting** now, carve the DV-identity
   re-founding out as a separate checkpoint. Built `agreement-matrix.ts` (additive, mirrors
   V4→V5): `computeAgreementMatrix` over `loadFixtureOutcomes` → `partitionByAgreement` (same
   primitive as the V5 manifest). Three verdict-free views: pairwise co-classing, per-engine
   capability + fork-participation + singleton-isolation, fork-shapes. The override-based
   `divergence-matrix.ts` + `seedCatalogue` + the whole `history`/`dv-lifecycle` are **left
   untouched (quarantined)**. Validated live (1955 tests): pairwise now measures reality
   (lattice↔gsheets 93.1%; libreoffice ~0% with everyone — the blank tail, quantified).
3. **DV-identity philosophy review** (no code) → two ratified design moves, captured in docs:
   - **`cause`/annotations go out-of-band** (refines CP2; note added to
     `comparison-output-contract-2026-06-17.md`). The review surfaced a three-way distinction:
     observed (relation) / authored-descriptive-with-gravity (`cause`) / authored-normative
     (oracle). `cause` is interpretation, not observation — sitting in the manifest it reads as
     fact. So the manifest goes **observation-only** (partition + capability).
   - **The full annotation-layer re-placement** (`annotation-layer-design-2026-06-19.md`, `e2e3ea46`).
     The principle extended from "no correct value" to **"no authority over meaning."** Three
     layers: assay observes → **edit-shell** collects attributed scoped explanations → sheets-wiki
     renders. Annotations are out-of-band, attributed, sticky-id (clusterKey retires), and
     **scoped not per-fork** (a shared explanation IS a cluster; clustering is interpretation).
     Coverage/staleness = derived reads, never write-cascades. Grounded in an edit-shell +
     sheets-wiki investigation (the where-it-lives call: edit-shell, not the assay repo, not the wiki).

## Current state

- **Branch** `integrate/assay-onto-main`; 3 commits this session (`4bd4c8d5`, `f7634a7c`,
  `e2e3ea46`), **all LOCAL**. Tree clean. **158 assay tests green**; tsc + prettier clean.
- The shipped V5 manifest still **emits `annotations` + fork-`cause`** — that is the
  now-superseded shape; the first build increment removes it (below).

## RESUME — gap-review, then build increment #1

The maintainer wants a **brief gap-review** before coding. Candidate gaps to check (most are
believed closed — confirm, don't re-litigate the ratified design):

- **Is anything already consuming `ManifestV5.annotations` / fork-`cause`?** sheets-wiki still
  reads V4, so likely nothing reads V5 annotations yet → safe to drop. Confirm.
- **`ManifestV5FunctionEntry.forks`** currently = `[...forkedCases, ...annotationIds]`; the
  rework makes it observed case-refs only. Confirm no consumer depends on the annotation ids.
- **Keep the `no-data` operational `cause`** (`EngineObservation`) — only the *fork*-cause
  (the annotation) leaves. Don't over-purge.
- **The 255 DVs / `links.divergence` / `history`+`seed-catalogue`** are **NOT** in increment #1
  (they're the edit-shell migration). #1 only changes the manifest *output*. Don't conflate.
- **Predicate-scope matcher, edit-shell annotation schema, lifecycle-as-stability** are all
  genuinely deferred to later increments — confirm they're out of #1's scope.

Then **build increment #1 — manifest observation-only rework** (assay + contracts, contained):
drop `annotations` + `ManifestForkAnnotation` + fork-`cause` from the V5 schema
(`contracts/src/index.ts`) and `buildManifestV5` (`build-v5.ts`); `FunctionEntry.forks` →
observed forked-case-refs only; move the annotation-gating tests out of `build-v5.test.ts`;
keep the `no-data` operational cause. Re-run the live manifest to confirm it's observation-only
(grep the output: no fork-`cause`/`annotation`).

## The build pieces (roughly independent; #1 is next)

1. **Manifest observation-only rework** — assay/contracts, contained. ← next.
2. **`divergence→forked` rename + §4 quarantine sweep** — assay; the held rename (now unblocked,
   multiplicity-model is decided). The deepest target was the matrix (done in step 6).
3. **edit-shell annotation store + the 255-DV migration** — its own design+build on
   `packages/edit-shell` (D1 table `assay_fork_annotations`, attributed; reuse the
   submit/review/runner rails). The in-repo DV-lifecycle/seed-catalogue retires or becomes a
   DB flow here.
4. **Website rework** — sheets-wiki, 10 V4→V5 break sites (named in the design doc §6).
   Deferred; **not a migration gate** (settled — reworked, not preserved).

## Backlog (unchanged, lower priority)

- Floor tail: lattice + libreoffice regen (need their envs — they're the blank-tail in the
  matrix/manifest); `lift.ts`/`legacyToOutcome` retirement.
- gsheets: 404 default spreadsheet id; `drive.file` scope (leaked scratch sheets); a regression
  test for the CP1 read sub-batching fix.
- The `Cause`-enum symmetry audit (directional causes → capability axis) — folds into #1/#2.
- Deferred comparison machinery: the digest (Move 1), the result fingerprint (equality #5), O4.
- Push is still maintainer-gated (all session work local).

---

## Continuation — 2026-06-20 (later session)

The gap-review + build increments above were executed, and CP3 increment #3 was scoped and
its design (3a) drafted + ratified §1–§7. **All local; nothing pushed. Tree clean** (the only
uncommitted file is the gitignored `.coord-status.md`). assay 158 tests + contracts 40 green;
tsc + prettier clean throughout.

**Shipped this session (commits, oldest→newest):**

1. `737c0c70` **increment #1 — ManifestV5 observation-only.** Dropped `ManifestForkAnnotation`
   + `ManifestV5.annotations` from contracts and `build-v5.ts`; `functions.forks` = observed
   case-refs only; kept the `no-data` operational cause. Validated live (520 fns; no
   annotations; all 21 cause-bearing obs are `no-data`).
2. `331f3c8e` **increment #2 — `divergence → forked`** in the live relation layer. `Divergence`
   → `AgreementPartition`, `isDivergent` → `isForked`, `detectDivergence` → `partitionCase`
   (compute-only; caller gates on `isForked`), `RunResult.divergences`/CLI labels → `forks`/
   `FORKED`/`Forks:`. ~8 files; the fossil (`divergence-matrix`/`history`/`seedCatalogue`) and
   the product name (`divergences/`, `DvEntry`, `DV-####`) deliberately untouched. Verified live.
3. `09f4e804 → 2eea3a91` **increment #3 scoped + the 3a design doc** —
   `docs/annotation-store-design-2026-06-20.md`. Built from two Explore investigations
   (edit-shell rails + the in-repo DV machinery). Ratified section-by-section §1–§7; refined
   live by the maintainer (case-tags as a matcher-free predicate surface; `scope` as a clause
   LIST; annotations history-agnostic). Then an **adversarial review pass** (findings R1–R4) and
   a **plain-language rewrite** (the current doc; R1–R4 are marked-open inline).

**RESUME — waiting on the maintainer's read of the 3a design.** They will return with thoughts.
Open and waiting on their verdict:

- **§8** — confirm edit-shell taking a `@cartularium/contracts` dependency (its first such edge).
- **§9** — the annotation review gate: publish-on-sign vs a light maintainer review.
- **R1** — publishing test `tags` to the manifest needs a hygiene gate (the corpus already has
  outcome-claim tags: `excel-only`, `divergence`, `coercion-divergence`). Decide gate vs sweep.
- **R2** — stop claiming the DV-lifecycle "retires fully": the "did this engine change?"
  reconstruction has a case-ref-vs-internal-key mismatch and conflates rename with convergence;
  retirement is coupled to an undesigned results-history substrate.
- **R3** — add a "dangling-ref vs converged" signal to the coverage view (3d).

Once §8/§9 + R1–R4 are decided: fold them into the doc, mark 3a complete, then **3b** (contracts
DTO + D1 migration + CRUD API). The build worktree/branch is chosen at 3b.

**Read order for the next session:** this continuation → `annotation-store-design-2026-06-20.md`
(the "Review state" block names R1–R4) → the comparison-principles memory. The adversarial
reviewer agent `af12d642250c09afd` is still live for follow-up.
